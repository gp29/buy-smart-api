'use strict';

const config = require('./../config');

/*
 * Related collection name with a database & schema
 */
module.exports = {
    dbSchema: {
        users: 'User',
        admins: 'Admin',
        products: 'Product',
        banners: 'Banner',
        results: 'Result',
    }
};